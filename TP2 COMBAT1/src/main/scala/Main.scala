import java.util.ArrayList

import org.apache.spark.{SparkConf, SparkContext}

   object Main extends App{
     val p=new Program();
     p.run();

   /* val graph = initialization(sc);
    while (true)
    {
      val msg=graph.aggregateMessages(sendMessages,mergeMessages);
    }*/



}
